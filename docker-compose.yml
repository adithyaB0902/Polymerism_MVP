services:
  polymemsim:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - polymemsim_data:/app/data
      - polymemsim_models:/app/models_artifacts

volumes:
  polymemsim_data:
  polymemsim_models:
